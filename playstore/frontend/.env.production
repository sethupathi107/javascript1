# Point this at the deployed backend's absolute origin once it exists.
# NOTE: the backend currently has no CORS middleware — it must allow this
# frontend's origin before production builds can talk to it cross-origin.
VITE_API_URL=https://api.example.com/v1
